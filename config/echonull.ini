# Device selectors accept "default", an exact endpoint ID, or a unique
# case-insensitive part of the Windows friendly name.
microphone = Razer Seiren V3 Chroma
reference = AW2725Q
output = CABLE Input

# Leave blank to auto-discover aec_48k.trtpkg below AFX_SDK_ROOT.
model =

sample_rate = 48000
delay_ms = 40
auto_delay = true
max_delay_ms = 250
intensity = 1.0

diagnostics_interval_ms = 1000
reconnect_delay_ms = 1500
timeline_capacity_ms = 4000

# Validation recordings are written here. An empty value disables recording
# during normal run mode; validate mode always records.
record_directory =
