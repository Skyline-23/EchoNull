# Runtime settings read by EchoNullPlugin.dll from the same directory.
# Use an absolute model path for Equalizer APO because audiodg.exe runs as
# LocalService and does not inherit the interactive user's environment.
model =
# Optional NVIDIA 48 kHz Background Noise Removal model.
noise_model =

sample_rate = 48000
delay_ms = 40
auto_delay = true
max_delay_ms = 250
timeline_capacity_ms = 4000
